import React from "react";
import styled from "styled-components";

export default styled.button`
  border: none;
  outline: none;
  font-size: inherit;
  color: inherit;
  padding: 0;
  cursor: pointer;
`;
